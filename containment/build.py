#from .types import BuildID
import docker

"""If the dockerfile exists then do not build, because the file is only written at the end of a successful build. The name of the file is the docker image id. It will be used as the argument to 'docker run'."""

def _combine(community, base):
    return df_contents


def build(community, base):
    return sha256nameddockerfile
